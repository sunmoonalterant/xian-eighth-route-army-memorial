# 游客端页面内容完整度（D11.5）

审计日期：2026-09-04。`complete` 表示当前页面已有与功能范围相称的公开内容链路；并不表示所有资料已完成史料人工核验。`partial` 表示页面可用但存在明确内容字段或时效缺口；`pending` 表示缺少可公开展示的核心资料。

| 路由 | 状态 | 当前内容来源 | 缺口或原因 |
| --- | --- | --- | --- |
| `/` | partial | 馆情、文物走 API + 静态回退；新闻静态/API；人物与时间线为整理提示 | 首页人物和时间线没有已审核资料；馆情与参观信息仍待复核 |
| `/museum` | partial | `/api/museum`，失败回退官方静态资料 | 馆情正文来源可追溯，但封面对应与当期资料仍需人工复核 |
| `/history` | pending | 静态空数组和“资料整理中”说明 | 无已审核历史事件；不得以空白时间填充演示事件 |
| `/relics` | partial | `/api/relics`，失败回退 9 条官方资料 | 9 条公开记录可浏览，但分类、年代和图片关联仍需逐条核验 |
| `/people` | pending | 静态空数组和“资料整理中”说明 | 无已审核的主题相关人物资料或可靠照片 |
| `/exhibitions` | partial | `/api/exhibitions`，静态去重回退 | 当前公开展览 2 条；原始采集含详情语义重复，需以详情页身份复核 |
| `/news` | partial | `/api/articles`，失败回退 5 条官网资讯 | 新闻数量足够课程展示，但发布日期、分类和封面仍应复核 |
| `/digital-museum` | pending | 平面布局示意与泛旧址保护来源 | 无可发布的具体院落热点、用途或一一对应图片；坐标不是测绘信息 |
| `/visit` | partial | 官方静态参观资料；预约功能另走 API | 地址尚空；开放时间、闭馆、交通、预约要求具时效性，需查当前官方公告 |
| `/search` | partial | 仅检索当前可用文物与新闻资料 | 人物、历史和院落没有已审核内容，按真实性边界不参与搜索 |
# D11.5C 更新

- `/people`：partial。25 位正式人物可浏览，图片尚未本地化，另有 4 位候选待审核。
- `/person/:id`：可用，读取正式人物 API；无图使用中性剪影。
- `/`：人物区已恢复；历史时间线仍 pending。
- `/search`：人物已恢复；历史事件未加入。
- `/history`：pending。
- `/digital-museum`：pending。

# D11.7 更新

- `/history`：complete。仅读取 `/api/history-events` 返回的 `verified + status=1` 正式事件；请求失败时明确提示，绝不回退到演示时间线。
- `/`：历史时间轴恢复，仅读取 `/api/history-events?featured=true` 的首页精选正式事件；数量不足时不以演示内容补齐。
- `/search`：仅将历史 API 返回的正式事件纳入搜索结果。
- `/digital-museum`：仍为 pending；本阶段不发布未审核院落资料或候选图片。

# D11.8 更新

- `/digital-museum`：partial。已恢复 4 个经人工批准的官网院落热点；底图尚未上传经审核的官方导览图，因此显示明确标注的课程设计示意图。
- 页面只读取 `/api/digital-museum`；API 失败或无公开院落时显示安全提示，不回退旧演示热点。
