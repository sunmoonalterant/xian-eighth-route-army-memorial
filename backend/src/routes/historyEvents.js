const express=require('express');const{createHistoryEventController}=require('../controllers/historyEventController');function router(pool,admin=false,auth){const r=express.Router();if(admin)r.use(auth);const c=createHistoryEventController(pool,admin);r.get('/',c.list);r.get('/:id',c.getOne);if(admin){r.post('/',c.create);r.put('/:id',c.update);r.delete('/:id',c.remove)}return r}module.exports=router
