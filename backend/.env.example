PORT=3000

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=xian_memorial

# Generate a long random secret for local development; do not commit the real value.
JWT_SECRET=
JWT_EXPIRES_IN=2h

# Local-only initial administrator seed values; do not commit the real password.
ADMIN_SEED_USERNAME=admin
ADMIN_SEED_PASSWORD=
ADMIN_SEED_DISPLAY_NAME=系统管理员
